<!-- requires board, size -->
<%= link_to board, class: "board-thumb" do %>
	<p><%= board.title %></p>
	
	<p><small><%= board.description %></small></p>
	<% if board.items.any? %>
		<%= cover(board.items.first)%>
	<% end %>

<% end %>