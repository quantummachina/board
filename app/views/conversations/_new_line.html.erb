<%= form_for @conversation.lines.build(conversation_id: @conversation.id) do |f|%>
	<%= f.hidden_field :conversation_id %>
	<%= f.text_area :text, rows: 3, class: 'span6' %>
	<%= f.submit "Enviar mensaje" %>
<% end %>