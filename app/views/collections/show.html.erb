<% provide :title, @board.title %> 
<% if !signed_in? %>
	<div class="idea textc">
		<h3>Para unirte a este proyecto <%= link_to 'inicia sesión', '/signin' %> o <%= link_to 'regístrate', root_path %>.</h3>
	</div>
	<br>
<% end %>

<div class="row">
	<div class="span12">
		<h1><%= @board.title %></h1>
		<p>Por <%=link_to @board.user.name, @board.user, rel: "tooltip", :"data-placement"=> "bottom", title: @board.user.about %>
			<% if @board.collaborators %>
				<% @board.collaborators.each do |c| %>


					<%=", "%> <%=link_to c.name, c, rel: "tooltip", :"data-placement"=> "bottom", title: c.about %>


				<% end %>
			<% end %>
			<%= link_to @board.category.name, nav_path(category: @board.category.id), class: "label"%></p>

			<%= render 'status' %>

			<%= render 'invite' if @board.status != 5 %>
			<%if signed_in? && (owner? || current_user.admin) %>
			<%= link_to "Editar proyecto", edit_collection_path(@board), class: "btn btn-small" %>
		<%end%><%= render 'promote'%>
		<br><br>

		<ul id="WoI" class="nav nav-tabs">
			<li class="active"><a href="#work" data-toggle="tab">Proyecto</a></li>
			<li><a href="#inspiration" data-toggle="tab">Inspiración</a></li>
		</ul>
		<div id="WoIContent" class="tab-content">
			<div class="tab-pane fade in active" id="work">
<%# division to work %>
				<div class="row">
					<div class="span6">

						<%if @board.cover != 0%>
							<%= wrap(default_OG_b(embedded_b(@board.items.find(@board.cover).content, 6))) %>
						<%else%>
							<%= image_tag(dummycover(@board.category_id))%>
						<%end%>
						<%= render 'share' %>
						<h2>La idea</h2>
						<p><%= @board.idea %></p>
						<% if @board.description == ''%>
							<% if signed_in? && owner? %>
								<h2>Descripción</h2>
								<p><%= link_to '¡Por favor provee una descripción de tu proyecto!', edit_collection_path(@board) %></p>
							<%end%>
						<%else%>
							<h2>Descripción</h2>
							<p><%= @board.description %></p>
						<%end%>

						<%if @board.status == 5%>
							<%= render 'finished' %>
							
						<%else%>

							<h2>Progreso</h2>
						<%end %>

							<% if collaborates? %><%=render 'shared/add_item', kind: 1 %><% end%>
						

						<%= render 'wcollections' %>
						<br>
						<br>
						<br>
					</div>
<%#rigt side%>
					<div class="span5 offset1">
						<%if @board.status != 5 %>
							<%= render 'collaborate'%>
						<%else%>
							<%= render 'finishlinks'%>
						<% end %>
						<hr>
						<%= render 'comments'%>
					</div>
				</div>
			</div>
<%#Division to inspiration%>
			<div class="tab-pane fade" id="inspiration">
				<% if collaborates? %><%=render 'shared/add_item', kind: 0 %><% end%>
				<%= render 'icollections' %>
				<br>

			</div>
		</div>
	</div>
</div>

<% if signed_in? && current_user.extra.newboard %>
	<script type="text/javascript">
	    $(window).load(function(){
	        $('#invite2').modal('show');
	    });
	</script>
	<%current_user.extra.toggle!(:newboard)%>
<%end%>