<% if owner? && @board.status != 5 %>

	<ul class="nav nav-pills">
	<li class="dropdown">
	  <a class="dropdown-toggle" data-toggle="dropdown" href="#"> Estatus: <%= status(@board.status) %> <b class="caret"></b></a>
	  <ul id="menu1" class="dropdown-menu">
	    <li><%= link_to controller: 'collections', action: 'switchstat', id: @board.id do %>
	    	Poner en <%= if @board.status == 1 then 'curso' else 'espera' end %><%end%>
	    </li>
	    <li class="divider"></li>
	    <li><%= link_to 'Marcar finalizado', {controller: 'collections', action: 'markfinished', id: @board.id}, confirm: "¿Estás seguro? Si marcas tu proyecto como terminado no podrás cambiar su estatus de nuevo."%>
	    </li>
	  </ul>
	</li>
	</ul> 

<%else%>

	Estatus: <%= status(@board.status) %>

<%end%>