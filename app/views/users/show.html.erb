<div class="row-fluid">
	<div class="span12">
		<h1><%= @user.name + (if @user.name.last != "s" and @user.name.last != "z" and @user.name.last != "S" and @user.name.last != "Z" then "'s" else "'" end) + " Lab"%></h1>
	</br>
		<%= if @user == @user then render 'collections/create_board' end %>
		<hr>
		<h2>Owned</h2>
		<%= render 'shared/boards', boards:@boards %>
		<hr>
		<h2>Collaborating</h2>
		<%= render 'shared/boards', boards:@collabs %>
	</div>
</div>