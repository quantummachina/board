
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :email %>
  <%= f.text_field :email, disabled: true %>

  <%= f.label :password, 'New Password' %>
  <%= f.password_field :password %>

  <%= f.label :password_confirmation, "Confirm Password" %>
  <%= f.password_field :password_confirmation %>

  <hr>
  <%= f.label :city %>
  <%= f.text_field :city %>

  <%= f.label :category_id, 'Creative fields' %>
  <%= f.select :category_id, options_from_collection_for_select(@categories, "id", "name", selected: @user.category_id)  %>
  <%= f.select :subcategory_id, options_from_collection_for_select(@categories, "id", "name", selected: @user.subcategory_id)  %>
  <%= f.label :about %>
  <%= f.text_area :about, maxlength: 200, placeholder:"Tell us about you. What do you do? What are your interests?", rows: 4, class: 'span5' %>

  <%= f.label :link_a, 'Where can we find more about you?' %>
  <%= f.text_field :link_a, placeholder: 'Facebook, Twitter, web page...' %>
<%= f.text_field :link_b, placeholder: 'Facebook, Twitter, web page...' %>
<%= f.text_field :link_c, placeholder: 'Facebook, Twitter, web page...' %>
<%= f.text_field :link_d, placeholder: 'Facebook, Twitter, web page...' %>